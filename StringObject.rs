fn main() {
    let empty_string = String::new();
    println!("length is {}", empty_string.len());

    let content_string = String::from("sample");
    println!("length is {}", content_string.len());
}